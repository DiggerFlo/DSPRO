"""Embedding pipeline — generates vector embeddings for all chunks."""


def embed() -> None:
    """Generate embeddings for chunks in data/chunks/ and save to data/embeddings/."""
    raise NotImplementedError("Implement embedding logic here.")


if __name__ == "__main__":
    embed()
